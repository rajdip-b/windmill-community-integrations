{
	"description": "Accelo",
	"schema": {
		"$schema": "https://json-schema.org/draft/2020-12/schema",
		"type": "object",
		"properties": {
			"deployment": {
				"type": "string",
				"description": "The deployment URL of your Accelo instance. For example, 'https://example.accelo.com'."
			},
			"clientId": {
				"type": "string",
				"description": "The client ID of your Accelo application."
			},
			"clientSecret": {
				"type": "string",
				"description": "The client secret of your Accelo application."
			}
		},
		"required": ["deployment", "clientId", "clientSecret"]
	}
}
